version: 0.2

phases:
  install:
    commands:
      - apt-get update -y
      - apt-get install zip -y
      - ls -la
      - zip github.zip start_instance.py
      - aws lambda update-function-code --function-name start --zip-file fileb://github.zip --publish